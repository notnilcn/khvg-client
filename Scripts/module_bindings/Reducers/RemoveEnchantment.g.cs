// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void RemoveEnchantmentHandler(ReducerEventContext ctx, uint slotIndex, string enchantmentId);
        public event RemoveEnchantmentHandler? OnRemoveEnchantment;

        public void RemoveEnchantment(uint slotIndex, string enchantmentId)
        {
            conn.InternalCallReducer(new Reducer.RemoveEnchantment(slotIndex, enchantmentId));
        }

        public bool InvokeRemoveEnchantment(ReducerEventContext ctx, Reducer.RemoveEnchantment args)
        {
            if (OnRemoveEnchantment == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnRemoveEnchantment(
                ctx,
                args.SlotIndex,
                args.EnchantmentId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class RemoveEnchantment : Reducer, IReducerArgs
        {
            [DataMember(Name = "slot_index")]
            public uint SlotIndex;
            [DataMember(Name = "enchantment_id")]
            public string EnchantmentId;

            public RemoveEnchantment(
                uint SlotIndex,
                string EnchantmentId
            )
            {
                this.SlotIndex = SlotIndex;
                this.EnchantmentId = EnchantmentId;
            }

            public RemoveEnchantment()
            {
                this.EnchantmentId = "";
            }

            string IReducerArgs.ReducerName => "remove_enchantment";
        }
    }
}
