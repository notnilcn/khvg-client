#nullable enable

/// <summary>
/// Decor prop layer of a TileComponent (z=3): the prop sprite itself, drawn above its shadow.
/// Behavior lives in DecorLayerComponent.
/// </summary>
public partial class DecorComponent : DecorLayerComponent
{
}
